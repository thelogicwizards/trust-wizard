feat: implement Cross-Platform Tauri v2 Multi-Builds

- Initialized Tauri 2.0 configuration for macOS desktop, iOS, and Android
- Added CSS safe-area-insets and viewport-fit rules to prevent UI from overlapping mobile notches
- Replaced blocked `window.open()` popups with React modal overlays to restore Document Printing and PDF Viewing capability on Apple WebKit browsers
- Integrated Tauri `@tauri-apps/plugin-fs` and `@tauri-apps/plugin-dialog` to replace standard PWA generic file streaming with native OS "Save-As" file system pickers for macOS and mobile platforms
- Resolved Android WebView ghost-click unmounting bugs on the Document Wizard
- Resolved CSS iOS fixed positioning stacking context inheritance issues
